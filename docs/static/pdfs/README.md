# PDF placeholder

The camera-ready paper PDF will be added here as `swiftcad.pdf` once available.

The "Paper" button on the project page (`docs/index.html`) is currently disabled
and points to `static/pdfs/swiftcad.pdf` so it will resolve automatically once
the file is dropped in this directory. Remember to also un-disable the button
in `index.html` at that point.
